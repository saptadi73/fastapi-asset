# Asset Management API — baseline dependencies
# Final exact pins/hashes should be generated after compatibility testing.

# Web API and ASGI
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
gunicorn>=23,<24
python-multipart>=0.0.20,<1.0

# Configuration and validation
pydantic>=2.10,<3.0
pydantic-settings>=2.7,<3.0
email-validator>=2.2,<3.0
python-dotenv>=1.0,<2.0

# PostgreSQL 18, ORM, and migrations
SQLAlchemy[asyncio]>=2.0.36,<3.0
alembic>=1.14,<2.0
asyncpg>=0.30,<1.0
psycopg[binary,pool]>=3.2,<4.0
greenlet>=3.1,<4.0

# Authentication, authorization, and cryptography
PyJWT[crypto]>=2.10,<3.0
argon2-cffi>=23.1,<26.0
cryptography>=44,<46

# HTTP integration and resilience
httpx>=0.28,<1.0
tenacity>=9,<10

# Serialization, date/time, and identifiers
orjson>=3.10,<4.0
python-dateutil>=2.9,<3.0

# Object storage, documents, QR, barcode, image processing, and malware scan
boto3>=1.40,<2.0
qrcode[pil]>=8.0,<9.0
Pillow>=11,<13
python-magic>=0.4.27,<1.0
filetype>=1.2,<2.0
clamd>=1.0,<2.0
# pillow-heif>=1.1,<2.0  # enable only when HEIC/HEIF upload is required

# Structured logging and observability
structlog>=25,<26
prometheus-client>=0.21,<1.0

# Scheduling; activate distributed queue dependencies only when required
APScheduler>=3.11,<4.0
# celery>=5.4,<6.0
# redis>=5.2,<7.0

# Testing
pytest>=8.3,<9.0
pytest-asyncio>=0.25,<2.0
pytest-cov>=6,<8
pytest-mock>=3.14,<4.0
factory-boy>=3.3,<4.0
faker>=33,<40
respx>=0.22,<1.0
testcontainers[postgres]>=4.9,<5.0

# Code quality, typing, and security checks
ruff>=0.9,<1.0
mypy>=1.14,<2.0
pre-commit>=4,<5
pip-audit>=2.7,<3.0

# Type information
types-python-dateutil>=2.9,<3.0
